[project]
name = "org-google"
version = "0.1.0"
description = "Sync org-mode files with Google Slides/Docs"
requires-python = ">=3.10"
dependencies = [
    "google-auth",
    "google-auth-oauthlib",
    "google-api-python-client",
]

[tool.uv]
dev-dependencies = []
